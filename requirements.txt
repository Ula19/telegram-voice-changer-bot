# Основные зависимости
aiogram==3.26.0
SQLAlchemy==2.0.38
asyncpg==0.30.0
alembic==1.14.1
pydantic-settings==2.8.1

# HTTP-клиент (используется aiogram)
aiohttp==3.11.11

# Ускорение asyncio (не работает на Windows!)
uvloop==0.22.1
